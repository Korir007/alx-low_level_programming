#include "main.h"

/**
 * _islower - Checks c if it is lower case
 * @g: The character to prin
 *
 * Return: 1 if true and 0 if false
 */
int _islower(int g)
{
	if (g < 97)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
